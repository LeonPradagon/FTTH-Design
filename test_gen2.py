from backend.api.routes.generation import _run_generator_logic
import glob

# Find the boundary kml
boundary_path = "/Users/jessicakimberly/Downloads/ftth_design_generator/cache/test_boundary.kml" # wait, let's find a real one
# The user's upload goes to a temp file, but I don't have it.
